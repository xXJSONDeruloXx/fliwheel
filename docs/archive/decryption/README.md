# Archived package-decryption research

These notes cover FairPlay, encrypted `.ipg` packages, and device-key
experiments. The current project consumes decrypted `Games_RO` bundles, so this
material is retained as background rather than part of the active workflow.
