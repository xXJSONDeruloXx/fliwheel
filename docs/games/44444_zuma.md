# Zuma (Bundle 44444)

**Status:** ⚠️ POPCAP DMA/CONTENT PARTIAL | **Evidence:** 50M-cycle probe completes a full DMA-buffer write but presents a noisy top fragment | **Engine:** PopCap Engine

## Quick Start
```bash
# Partially works - renders loading text and a partial DMA scene
./target/release/eapp /Users/kurt/Downloads/16-ipod-games/Games_RO/44444
```

## Issue
The current HLE reaches eight GL draws and writes all 153,600 bytes of the
320x240 DMA buffer. The final DMA-only capture contains 27,352 non-zero pixels:
a noisy top-of-screen fragment with the remainder black. The bounded run exits
cleanly; the older “DMA completion never arrives” diagnosis is stale.

## Bundle Info
- **Executable:** `Zuma_1_1_2563298.bin` (eapp format)
- **Import Modules:** AsyncFileIO, Audio, CTC, CTH, InputEvents, Metadata, Settings, TTC, TTT, ZHHHMHTT

## Notable
Many exotic import module names (CTC, CTH, TTC, TTT, ZHHHMHTT) — likely
obfuscated PopCap engine modules.

## Fix Needed
Resolve the shared PopCap aperture/data contract and then repair the title's
texture/RO upload and board composition. `0x1402000c` is an observed pixel
offset in the current trace, not an established completion register.

## Environment
```bash
CLICKY_EXPERIMENTAL_GL_HLE=1
CLICKY_GL_GATE_B=1
CLICKY_GL_LIVE_CONTINUOUS=1
CLICKY_GL_PRESENT_VFLIP=1
```

Focused evidence: [PopCap DMA contract probe](../game_tests/20260826_popcap_dma_contract.md).
