# Zuma (Bundle 44444)

**Status:** 🟡 POPCAP BOARD/OVERLAY PARTIAL | **Evidence:** 30M-cycle interactive probe presents an upright stone-framed board and title overlays, but gameplay composition is incomplete | **Engine:** PopCap Engine

## Quick Start
```bash
# Set GAMES_RO to the directory containing the decrypted bundles.
GAMES_RO=/path/to/Games_RO
./target/release/eapp "$GAMES_RO/44444"
```

## Issue
The current HLE reaches the resource-backed GL path and exits cleanly. A
30,000,000-cycle interactive probe presents a complete stone-framed board,
the `PRESS SELECT TO ENTER THE TEMPLE` prompt, stage text, and several title
overlays. The menu and board geometry are now upright and use the guest's
bound texture names, but the playable marble track, launcher, and full menu
composition are still missing or malformed.

## Bundle Info
- **Executable:** `Zuma_1_1_2563298.bin` (eapp format)
- **Import Modules:** AsyncFileIO, Audio, CTC, CTH, InputEvents, Metadata, Settings, TTC, TTT, ZHHHMHTT

## Notable
Many exotic import module names (CTC, CTH, TTC, TTT, ZHHHMHTT) — likely
obfuscated PopCap engine modules.

## Fix Needed
Continue from the verified board-entry boundary: trace the remaining
material/UV pairs, then drive Select and the clickwheel through board entry and
marble gameplay. `0x1402000c` is an observed pixel offset in the historical
trace, not an established completion register.

## Environment
```bash
CLICKY_EXPERIMENTAL_GL_HLE=1
CLICKY_GL_GATE_B=1
CLICKY_GL_LIVE_CONTINUOUS=1
# PopCap titles default to the guest screen origin; set this explicitly only
# for orientation A/B experiments.
```

Focused evidence: [PopCap DMA contract probe](../game_tests/20260826_popcap_dma_contract.md).
