# Cubis 2 (Bundle 99999)

**Status:** 🟡 BOARD RENDERING + WAV EVENTS VERIFIED | **Evidence:** current live GL HLE reaches a readable main menu and a complete underwater/isometric level scene; the wheel/select route moves the board state, all 21 effect WAVs map to source handles, and the headed sink plays observed events, while matching, full levels, music, save, and persistence remain open | **Engine:** Cubis 2 runtime

## Quick Start
```bash
./scripts/games/cubis2.sh
./scripts/games/cubis2.sh --timeout 15    # auto-terminate
./scripts/games/cubis2.sh --headless      # no window
```

## Bundle Info
- **Executable:** `Cubis2_1_1_2563292.bin` (eapp format)
- **Splash:** `Cubis2.raw.lcd5` (RGB565)
- **Asset Format:** `.pix` (14 files) + `.ipd` (16 files)

## Assets
- **Textures:** `.pix` files for common game elements
- **Audio:** 21 `.wav` files
- **Background music:** `g.m4a`
- **Resources:** `fonts/`, `desert/`, `jewel/`, `classic/`, `images/`

## Visual
The current runner reaches the complete main menu at guest frame 64 after the
resource-loading phase. The menu includes the background, Cubis logo, New Game,
Volume, Options, High Scores, and Help/Exit labels. The first corrected default
orientation capture is recorded in
[`20260829_cubis2_menu_orientation.md`](../game_tests/20260829_cubis2_menu_orientation.md).

The arcade board and its cube-matching interactions still need a title-specific
playthrough. The current exploratory route already renders the underwater board,
cube stacks, HUD, objective prompt, and moving cube state; it has not yet
certified a completed match. MobyGames' reference screenshots show the expected in-game layout:
an isometric cube field, left-side score/cube/time HUD, and menu/options
controls.

The audio source/trigger path is recorded in
[`20260829_cubis2_audio_mapping.md`](../game_tests/20260829_cubis2_audio_mapping.md).

## Environment
```bash
FLIWHEEL_EXPERIMENTAL_GL_HLE=1
FLIWHEEL_GL_GATE_B=1
FLIWHEEL_GL_LIVE_CONTINUOUS=1
```

`99999` is now included in the automatic PopCap-style screen-origin defaults;
`FLIWHEEL_GL_PRESENT_VFLIP` remains available as an explicit diagnostic
override.
