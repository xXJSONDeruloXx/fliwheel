# Eapp Matrix Hardening

Document the 2026-06-20 headed matrix and iteratively harden the general clickwheel eapp runtime using cross-game evidence.

## Goals
- Preserve a clear, cited matrix of current per-game status, artifacts, logs, visual observations, performance, and blockers.
- Prefer general ABI/renderer/runtime fixes over title-specific hacks.
- After each fix, rerun targeted headed/headless smoke tests and record evidence.
- Keep Tetris as the golden regression while improving sibling games.

## Checklist
- [x] Add headed matrix documentation with artifact root and per-game log/capture citations.
- [x] Implement/decode `src_fmt=0x190a pix_type=0x1401` upload path and validate against Cubis 2 / LOST.
- [x] Investigate and fix shared Bejeweled/Zuma unmapped write at `0x1080000c`.
- [x] Implement `OpenGLES:37 mode=5` or accurately identify/guard it for Texas Hold'em.
- [ ] Improve UV/upload matching for Mahjong / PAC-MAN / Ms. PAC-MAN / Mini Golf / Tetris.
  - [x] Capture and document Mahjong ordinal-45 descriptor/upload evidence.
  - [x] Decode Mahjong ordinal-45 descriptor layout enough to build guarded real resource uploads and recover pre-bind UV arrays without hardcoded texture/zero-UV fallbacks.
  - [x] Fix ordinal-37 `mode=5` (Texas Hold'em) UV decode: vertex arrays persist across material binds, so use the epoch-agnostic UV decode (matches ordinal-38 path and GL semantics). `no live upload matched` count dropped `164 -> 1`; rasterized strips `-> 165`. Tetris golden regression unaffected.
  - [x] Associate ordinal-99 uploads with their GL texture names for the Tetris-family engine: ord45 descriptor word1 IS the GL tex name (no separate glBindTexture ordinal needed). Tag `LiveGlUpload.tex_name` from ord45, prefer `select_upload_by_tex_name(handle)` at draw time, fall back to existing dim/UV heuristics. Tetris 38/38 uploads tagged; Holdem uploads still untagged (its ord45 fires once for many uploads to distinct textures — needs the Holdem-specific bind ordinal). No regression vs baseline; 14 lib + 15 eapp_gl_decode tests pass.
  - [~] Tag Holdem uploads to distinct GL texture names — **investigated, will NOT patch** (reclassified, see Verification): Holdem ord45 fires per-upload but points to zeroed work-RAM scratch (no tex name); there is no dedicated bind-before-upload ordinal. Only `ord159` binds tex names at draw time (handles `0x23`/`0x6`, one per frame). The 9 uploads (9 distinct widths) cannot be 1:1 mapped to `{0x23,0x6}` from registers. Critically the 31 skips are `UV span None` (a zero-UV decode gap), so tex-name tagging would not reduce skips and would regress the 4568 dim-matched draws (6 uploads → 1 handle, "most recent wins" mis-associates). Existing per-draw dim/UV heuristic is the correct Holdem path. Real blocker = zero-UV strips, folded into the UV-matching workstream.
  - [~] Improve UV/upload matching for PAC-MAN / Ms. PAC-MAN / Mini Golf (verify whether they use the Tetris ord45 tex-name path, the Mahjong ord45 descriptor path, or something else; capture GL traces per title). **Investigated, evidence recorded**: none of the three uses `ord45`. PAC-MAN (`AAAAA`) & Ms. PAC-MAN (`14004`) are the same Tetris-family ord37 engine with 1 ord99 upload/frame (GL_RGBA 512-wide), no ord45; matching via ord99 dim heuristic works (105,696 / 35,325 rasterized); remaining skips are `UV span None` (zero-UV) → general UV-decode workstream. Mini Golf (`88888`) has zero ord99 and zero ord45 (offline file-backed textures); 16,887 rasterized; only skip is `UV span Some` for handle `0x27` (valid UVs, no live upload to match) → fix would route such handles to file-backed texture, not skip. No code patched (neither ord45 path applies).
- [x] Investigate lifecycle/timer/settings/resource blockers for Sims Bowling, Sims Pool, Sudoku, Royal Solitaire, iQuiz, Vortex.
  - [x] Classify no-capture titles from matrix logs.
  - [x] Implement/identify ordinal `38` draw path for Sims/Sudoku/Solitaire family as `DrawElements(GL_TRIANGLE_STRIP, count, GL_UNSIGNED_SHORT, indices)`.
  - [x] Identify ordinal `149` draw-adjacent state semantics; classified as a safe no-op per-draw bind of a fixed runtime state object (constant args `r0=0 r1=1 r2=0 r3=<BSS ptr>`, between material bind `159` and `DrawElements` `38`). Sims runs 0 fatals without it; full decode is low-priority.
  - [x] Investigate iQuiz/Vortex early fatal object-layout writes; root-caused both to a null destination buffer/object pointer (Vortex struct-fill reads `[object+4]`=null at `pc=0x18014d58`; iQuiz memcpy dest null at `pc=0x18001b08`). Fix needs targeted per-game RE of the specific HLE call that should populate the buffer (Vortex: likely GL surface bind; iQuiz: likely a `Metadata` object provider). Added general register-dump-on-fault diagnostics. Not patched (no guessing pointer-backed buffers).
  - [~] Root-cause idle/no-capture for Sims Pool, Sudoku, Royal Solitaire (Sims Bowling now rasterizes; the siblings still produce no captures — determine if it's missing input/timer, missing resource, or a draw-state gap). **All three root-caused as input/timer idle, NOT runtime bugs** (iter-2 corrected iter-1's wrong "incomplete AsyncFileIO callback" claim). Sims Pool (`1500E`) trace `/tmp/clicky_simspool_trace_20260620_223209/simspool.log`: 2 ord99, 7 ord38, 14 ord159, 4498 rasterized, `splash_phase=0`, `first_hash_change=Some(4)` (4 distinct boot frames), `async=req:2 queued:2 callbacks:2 pending:0 staged:1` (async COMPLETED, not stuck). Royal Solitaire (`50514`) trace `/tmp/clicky_solitaire_trace_20260620_223344/solitaire.log`: 5 ord99, 17 ord38, 25 ord159, 1235 rasterized, `splash_phase=5`, `first_hash_change=Some(2)`, `async=...pending:0 staged:1`. Sudoku (`50513`) `/tmp/clicky_sudoku_trace_20260620_222919/sudoku.log`: 15,378 ord157 + 0 ord158; 1 ord38 strip; `splash_phase=15`, `first_hash_change=Some(2)`, async transitioned `pending:1 callbacks:0` → `pending:0 staged:1` (callback fired; NOT stuck). All three: `app_events=[]` throughout → headless harness injects no clickwheel input, so menu-driven titles boot-render then idle on a static title screen waiting for a start-button event. Sims Bowling (working sibling) keeps rasterizing thanks to an animated attract mode. The no-`ord158` GL lifecycle is NOT a frame-discard bug — `live_handle_draw_elements` (ord38) already calls `begin_frame()` implicitly; frames are `status=presented` (the `begin=@None` in `frame_diag` is only a missing-158 label artifact). Not patched (needs a headless input-injection harness; scoped deferred as a test-harness feature, not an eapp runtime fix).
- [ ] Rerun a full matrix and update docs.

## Verification
- Matrix artifact root: `/tmp/clicky_headed_matrix_unique_20260620_201555`
- Contact sheet: `/tmp/clicky_headed_matrix_unique_20260620_201555/contact_sheet.png`
- Build before matrix: `cargo build -p clicky-desktop --bin eapp` passed with existing warnings.
- Added full matrix docs to `docs/IPOD_GAMES_BRINGUP.md` with per-game logs/captures, performance estimates, and ranked blockers.
- Implemented `TextureFormat::LuminanceAlpha88` for GL ES `GL_LUMINANCE_ALPHA` (`0x190a`) + `GL_UNSIGNED_BYTE` (`0x1401`).
- `cargo test -p clicky-core --test eapp_gl_decode` passed.
- Targeted headed validation root: `/tmp/clicky_la_validate_20260620_202557`.
- Cubis 2 after fix: unsupported `0x190a/0x1401` count 0, skipped draws down from 18 to 2; latest screenshot `/tmp/clicky_la_validate_20260620_202557/99999_latest.png`.
- LOST after fix: unsupported `0x190a/0x1401` count 0 and early uploads decode as `Some(LuminanceAlpha88)`; still no completed GL frames in 7s.
- PopCap crash root cause: work RAM aperture too small. 8 MiB crashed at `0x1080000c`; 32 MiB moved Zuma to `0x1200000c`; 64 MiB survives 7s headed windows for Bejeweled and Zuma.
- 64 MiB validation root: `/tmp/clicky_ram64_validate_20260620_203011` with logs/screens for `55555`, `44444`, and `66666` Tetris regression.
- Implemented `OpenGLES:37 mode=5` as standard GL ES triangle strip. Validation root: `/tmp/clicky_mode5_validate_20260620_203634`; unsupported mode=5 count is now 0 for Texas Hold'em, remaining blocker is triangle-strip UV/upload matching for handle `0x23`.
- Mahjong UV/upload investigation: trace root `/tmp/clicky_mahjong_trace_20260620_203851`; JSON capture `/tmp/clicky_mahjong_capture2_20260620_204015/mahjong_gl.json`. Evidence suggested alternate `OpenGLES:45` descriptor/resource upload semantics; documented in `docs/IPOD_GAMES_BRINGUP.md`. Do not hardcode zero-UV fallbacks before decoding descriptor layout.
- Implemented guarded Mahjong-style ordinal-45 resource uploads. Decoded layout: `r1+0x04` points to texture object; object word 2 is material handle (`0x19`/`0x12`); word 4 is packed dimensions (`height << 16 | width`); word 9 is pixel pointer; word 10 is format-ish token (`0x8808`/`0x0801`, currently A8). Relaxed ordinal-37 UV material-epoch guard only for handles backed by ordinal-45 resource uploads, because Mahjong defines arrays before `159` material bind. Headless validation root `/tmp/clicky_ord45_validate_20260620_210929`: Mahjong logged 2 ordinal-45 uploads, 33,095 rasterized draws, 1 skip, 0 fatals; Tetris short regression logged 0 ordinal-45 uploads, 3,253 rasterized draws, 0 fatals. Headed validation root `/tmp/clicky_ord45_headed_20260620_211224`: Mahjong produced 33 PPM captures, 2 ordinal-45 uploads, 1,664 rasterized draw lines, 1 skip, 132 frame diagnostics, 0 fatals.
- Lifecycle/no-capture classification added to `docs/IPOD_GAMES_BRINGUP.md`: Sims Bowling/Pool, Sudoku, and Royal Solitaire use uploads/arrays followed by `159(...),149,38` rather than ordinal-37 draws; LOST now idles after LA fix with `159(h0xe)`; iQuiz/Vortex remain early fatal object-layout writes.
- Implemented ordinal `38` indexed draw-elements path. Evidence root `/tmp/clicky_ord38_capture2_20260620_204826`: `r0=0x5`, `r1=count`, `r2=0x1403`, `r3=index_ptr`. Headless validation root `/tmp/clicky_draw_elements_validate2_20260620_205657`: Sims Bowling logged 950 indexed draw rasterizations and 132 frame diagnostics with 0 fatals; Sudoku logged 1 indexed draw and 2 frame diagnostics; Tetris regression had 0 fatals. Headed validation root `/tmp/clicky_draw_elements_headed_20260620_205755`: Sims Bowling 5s headed run logged 234 indexed draw rasterizations and 9 capture files; Tetris 5s headed regression logged 20 capture files and 0 fatals.
- Added general register-dump-on-fault diagnostics (`fault regs pc=... fault_addr=... kind=... r0..r12 sp lr`) to the eapp memory-fault path; helps any future fatal, not just iQuiz/Vortex.
- iQuiz/Vortex fatal investigation: Vortex logs `/tmp/clicky_vortex_invest_20260620_211533/vortex{,_debug}.log`; iQuiz log `/tmp/clicky_iquiz_invest_20260620_211533/iquiz_debug.log`. Disassembly confirms Vortex faults in a struct-fill (`0x14d38`: `ldr r0,[r0,#4]` then `stmia r0!`, with `mov fp,#65536` field) and iQuiz faults in a 32-byte memcpy loop (`0x1af4`). Both dereference a null destination buffer. Register dumps: Vortex `r0=0x8 lr=0x00010000`, iQuiz `r0=0x10 lr=0x0`. Fix needs per-game RE of which HLE call populates the buffer (Vortex: likely GL surface bind; iQuiz: likely `Metadata` object provider); not patched to avoid guessing.
- Ordinal `149` classification: evidence `/tmp/clicky_ord149_invest_20260620_211533/sims.log` (2941 calls, constant args `r0=0 r1=1 r2=0 r3=0x1807cbc8`, between `159` and `38`). `r3` is beyond the file-backed image (BSS/runtime token) and the guest never derefs it, so the no-op is provably safe; full decode is low-priority.
- Implemented/decoded ord45 descriptor (Tetris/Holdem layout) → GL texture-name tagging. Evidence root `/tmp/clicky_bind_capture_20260620_220000/tetris_gl.json`: ord45 desc word1=GL tex name (e.g. `0x13`), word2=source_format, word4/5=w/h; equals the handle later bound by ord159. Validation root `/tmp/clicky_texname_validate_20260620_221500`: Tetris 38/38 uploads tagged `tex_name`, 4808 rasterized, 9 skipped, 0 fatals; Holdem 9 uploads untagged (ord45 fires once for many uploads), 2324 rasterized, 176 strips, 31 skipped, 0 fatals. Baseline `/tmp/clicky_texname_baseline_20260620_222700` confirms no regression (Tetris 4813/9 vs 4808/9; Holdem identical). 14 lib + 15 eapp_gl_decode tests pass.
- Holdem tex-name tagging investigation (reclassified, not patched): trace root `/tmp/clicky_holdem_trace_20260620_220951/holdem.log` (4534 GL trace lines, 9 ord99, 10 ord45); pointer-snapshot JSON `/tmp/clicky_holdem_json_20260620_221135/holdem_gl.json`. Corrected the prior "ord45 fires once" note — ord45 fires per-upload but `r1` points to zeroed work-RAM scratch (`0x180b6f2c..0x180b6f40`, +4 each), not a Tetris descriptor; no tex name. No dedicated bind-before-upload ordinal exists; only ord159 binds tex names at draw time (`0x23`/`0x6`, one per frame, alternating). 9 uploads have 9 distinct widths (`0x140,0x70,0x64,0x4c,0x30,0x2c` frame 0; three `0x200` in frames 8/11/14) and can't be 1:1 mapped to `{0x23,0x6}`. Holdem's 31 skips are `no live upload matched UV span None (handle=0x6)` — a zero-UV decode gap, not a tex-name gap; tagging would not reduce skips and would regress the 4568 dim-matched draws (6 uploads→1 handle, most-recent-wins mis-associates). tex_name stays `<none>` for Holdem by design; real blocker folded into UV-matching workstream. Holdem numbers unchanged (4568 rasterized, 121 skip lines, 9 uploads `<none>`), confirming no regression.
- PAC-MAN/Ms.PAC-MAN/Mini Golf UV/upload classification (evidence only, no patch): `AAAAA` trace `/tmp/clicky_pacman_trace_20260620_221704/pacman.log` (120M cyc, 0-40f): no ord45; 1 ord99/frame (GL_RGBA 512-wide); ord37 engine; ord159 binds 0x19/0x02; 105,696 rasterized, 124 skipped (`UV span None` zero-UV). `14004` trace `/tmp/clicky_mspacman_trace_20260620_222557/mspacman.log` (25M cyc, 0-8f): same engine; 1 ord99/frame; ord159 binds 0x02/0x03/0x19; 35,325 rasterized, 122 skipped (`UV span None`). `88888` trace `/tmp/clicky_minigolf_trace_20260620_222730/minigolf.log` (25M cyc, 0-8f): zero ord99 + zero ord45 (offline file-backed textures); 28 ord37; 16,887 rasterized; only skip = `UV span Some` for handle 0x27 (valid UVs, no live upload to match). Conclusion: none uses ord45; PAC siblings' gap = zero-UV decode workstream; Mini Golf gap = route no-live-upload handles to file-backed texture. No code patched (neither ord45 path applies).
- Sudoku idle root-cause (evidence only, not patched): trace `/tmp/clicky_sudoku_trace_20260620_222919/sudoku.log` (25M cyc, 0-20f). 15,378 `ord157` (present) calls, ZERO `ord158` (begin) → every present discarded; one indexed strip drawn (frame 1: `159(0x12),149,38` then `159(0x3),149`), then steady `159(0x3),149,157` loop with no further `38`. `EAPP_PROGRESS` shows `splash_phase=15 splash_flags=0x1080c102 async=req:1 queued:1 callbacks:1 pending:0 staged:1 app_event_head=0x0 app_events=[]`. Primary blocker = incomplete `AsyncFileIO` callback (1 req queued, never stages); secondary = no-ord158 GL lifecycle for this family (single `38` frame discarded). `ord157` regs `r2=0xbf800000 r3=0x3f800000` (floats -1.0/1.0), `r0=0x0` match surface-handle bind, not present. Royal Solitaire/Sims Pool likely share; need their own traces.
- **Iter-2 Sims Pool / Royal Solitaire / Sudoku idle root-cause CORRECTION & completion**: the iter-1 Sudoku "incomplete AsyncFileIO callback" diagnosis was WRONG. Sims Pool trace `/tmp/clicky_simspool_trace_20260620_223209/simspool.log` (25M cyc, 0-8f): 2 ord99, 7 ord38, 14 ord159, 4498 rasterized, `splash_phase=0`, `first_hash_change=Some(4)`, `async=req:2 queued:2 callbacks:2 pending:0 staged:1` (async COMPLETED). Royal Solitaire trace `/tmp/clicky_solitaire_trace_20260620_223344/solitaire.log` (25M cyc, 0-8f): 5 ord99, 17 ord38, 25 ord159, 1235 rasterized, `splash_phase=5`, `first_hash_change=Some(2)`, `async=...pending:0 staged:1`. Sudoku re-checked: async transitioned `pending:1 callbacks:0` → `pending:0 staged:1` (callback fired; NOT stuck); `first_hash_change=Some(2)`. All three: `app_events=[]` throughout (headless harness injects no clickwheel input). Root cause = menu-driven titles boot-render then idle on a static title screen waiting for a start-button event; Sims Bowling (working sibling) keeps rasterizing due to an animated attract mode. NOT an eapp runtime bug. The no-`ord158` GL lifecycle is also NOT a frame-discard bug — `live_handle_draw_elements` (ord38) at `clicky-core/src/sys/eapp/mod.rs:1873` already calls `begin_frame()` implicitly when no frame is active, so frames are `status=presented`; the `begin=@None` in `frame_diag` is only a missing-158 label artifact (it searches `ordinal_trace` for the 158 ordinal). Not patched (needs a headless input-injection harness; scoped deferred as a test-harness feature).
- **Iter-2 zero-UV decode code-path investigation (evidence only; no patch yet)**: located the "no live upload matched UV span None" skip in `clicky-core/src/sys/eapp/live_gl.rs:636` (quad path, `rasterize_quad_record`). For ord37 `mode=7` quads (Tetris/PAC-MAN/Ms. PAC-MAN family), `has_uv=false` arises at `clicky-core/src/sys/eapp/mod.rs:2161-2173` when BOTH (a) the explicit UV array (`arrays[1]`/`arrays[2]`) is absent/invalid AND (b) `live_decode_generated_uvs(state_ptr)` returns None (no texgen text object found for that handle) AND (c) `quad_groups==1`. For ord37 `mode=5` triangle strips (Texas Hold'em / Holdem) the same gap is at `mod.rs:1800-1813` (`live_decode_uvs_range_any_epoch`). PAC-MAN confirmation — `/tmp/clicky_pacman_trace_20260620_221704/pacman.log`: all 124 skips are `UV span None` for handles `0x19` (draw1) and `0x02` (draw38); the draws have a valid position array but no explicit UV array and no texgen object. The existing per-draw dim/UV heuristic matches the 105,696 rasterized draws correctly; only the no-UV draws skip. The correct general fix is to synthesize full-coverage default UVs (`[(0,0),(1,0),(1,1),(0,1)]`) for no-UV draws that DO have a matching live upload by dims/handle, instead of skipping — but this requires verifying it doesn't regress Tetris golden (whose no-UV draws may be intentional clears/solid-fills). Deferred to iter-3 for a guarded implementation + Tetris regression.

## Notes
- Matrix used headed 7s runs via `scripts/tetris.sh --no-build --timeout 7 --bundle <bundle>` with live GL enabled and captures/logs rooted per-game.
- Obvious high-value fixes, in order: 0x190a/0x1401 texture decode, 0x1080000c PopCap write, mode=5, UV/upload matching, lifecycle/idling titles.
- Resuming iteration 7+: previous loop completed with remaining work = Holdem tex-name tagging, PAC-MAN/Ms.PAC-MAN/Mini Golf UV matching, Sims Pool/Sudoku/Royal Solitaire idle root-cause, and full matrix rerun + doc update.